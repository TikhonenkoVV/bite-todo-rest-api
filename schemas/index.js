const userSchemas = require("./userSchemas");
const boardsSchema = require("./boardsSchema");
const columnsSchema = require("./columnsSchemas");
const tasksSchema = require("./tasksSchema");

module.exports = { userSchemas, boardsSchema, columnsSchema, tasksSchema };
