const User = require("./user");
const Token = require("./token");
const Board = require("./boards");
const Column = require("./column");
const Task = require("./tasks");

module.exports = { User, Token, Board, Column, Task };
